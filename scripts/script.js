// 簡単なインタラクション（今はログだけ。必要ならここに追加していく）
document.addEventListener("DOMContentLoaded", () => {
  console.log("hibikicode landing page ready.");

  // 例：スクロール時に軽いフェードインを付けたい場合の準備（今は未使用）
  // const observeTargets = document.querySelectorAll(".feature-card");
  // ...
});
